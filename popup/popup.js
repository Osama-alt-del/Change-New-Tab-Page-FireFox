function changeURL(){
    element = document.getElementById("inputURL");
    url = element.value;
    // I wanna see what the value is
    console.log(url);
    // export the url so that it can be seen by the other file.
    export url;
}

